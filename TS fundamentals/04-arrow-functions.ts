
// std js
let log = function(message) {
    console.log(message);
}

// TS arrow function
let doLog = (message) => {
    console.log(message);
}
// if only one line and one parameter (still a good idea to keep parameters parentheses)
let doLog2 = message => console.log(message);