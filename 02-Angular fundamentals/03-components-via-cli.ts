// If ng serve is running, open a new terminal

// cmd "ng g c <component name>"
// this is "angular generate component <name>"
// angular will create a new folder with the given name within the app folder, along with 4 files, a css for styling, an html for the template, a spec file for unit tests, and a ts file as the actual component. It also udpdates app.module.ts by registering the new component within it ()

// the ts file will be boiler plate, importing the Component decorator and OnInit interface from angular core. The Component decorator is applied to the class, along with a selector, the url of the template file, and the url of the stylesheet. 