// First, a component must be created, then it is to be registered in a module, and then an element must be added in the HTML markup.

// Check the "courses.component.ts" (words are seperated by hyphens, conventionally)

// Forgetting to register a component within a module will prevent angular from being able to recognize the custom element. Therefore, using the cli is a quicker and more reliable way to register a new component